%main.form-signin.col-9.mx-auto
  = form_with(model: [@ranking, @song], method: :post) do |f|
    %h4.my-2.fw-normal
      曲の情報を入力してください。
    .form-floating
      = f.text_field :title, class: 'form-control', required: true, id: 'title'
      %label{ for: 'title' } 曲名
    .form-floating
      = f.text_field :model, class: 'form-control', required: true, id: 'model', maxlength: 30
      %label{ for: 'model' } 機種名(30文字以内で入力してください)

    = f.submit '編集する', class: 'btn btn-lg btn-primary w-100 mt-2'
